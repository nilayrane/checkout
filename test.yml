- name: Hello World Sample
  hosts: all
  vars_prompt: 
    - name: "githubuser" 
      value: "nilayrane" 
      private: no 
    - name: "dhampya2497" 
      prompt: "Enter your github password" 
      private: yes 
  tasks:
    - name: Ensure remote URL does not contain credentials
      git_config:
        name: remote.origin.url
        value: https://github.com/nilayrane/test.git
        scope: local
        repo: /srv/checkout2
