- name: Hello World Sample
  hosts: all
  tasks:
    - name: Ansible delete directory example
      file:
        path: /tmp/checkout3
        state: absent
    - name: Create a ansible directory
      file:
        path: /tmp/checkout3
        state: directory
    - name: Git clone
      expect:
        command: git clone https://github.com/nilayrane/test.git /tmp/checkout3
        responses:
          Username: "{{ user }}" # Username is a regex
          Password: "{{ pass }}" # Password is a regex
